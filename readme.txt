nginx.script.sh : u can install and rung for static website by providing basic information

postfix :it is the servers that used to snd the email form linux machine to the user by runing my bash script u install the postfix
and all the configartion which need to run already write in this code 
per requriedment:
emailname
apps passwrd

monitoring_space.sh : you can check your RAM and disk space and also get the email if you install postfix 
RUN FOR BASH SCRIPT BY USING : bash filename
